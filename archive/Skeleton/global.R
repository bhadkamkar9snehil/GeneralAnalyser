# global.R
# Load necessary packages and define global objects or functions

# Core Shiny packages
library(shiny)
library(shinydashboard)

# shinydashboardPlus is required for the right-hand control bar
library(shinydashboardPlus)

# (Optional) Additional packages as placeholders
library(dplyr)
library(ggplot2)

# Global variables or helper functions go here if needed
# For example:
# global_data <- data.frame(x=1:10, y=rnorm(10))
# helper_function <- function(x) { return(x + 1) }
